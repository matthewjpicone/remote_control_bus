#pragma once

// Copy this file to credentials.h and enter the Wi-Fi network used for
// over-the-air firmware updates. credentials.h is intentionally gitignored.
const char* ssid = "YOUR_WIFI_SSID";
const char* password = "YOUR_WIFI_PASSWORD";
